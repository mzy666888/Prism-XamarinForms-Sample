<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="PrismSample.Views.MainPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="{Binding Title}">

    <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
        <Button Command="{Binding DisplayAlertCommand}" Text="DisplayAlert" />
        <Button Command="{Binding DisplayActionSheetCommand}" Text="DisplayActionSheet" />
        <Button Command="{Binding DisplayActionSheetUsingActionSheetButtonsCommand}" Text="DisplayActionSheet using ActionSheetButtons" />
    </StackLayout>

</ContentPage>